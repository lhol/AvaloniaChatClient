Features v0.4
- Active Chat Tab on the left side should be coloured
- The left panel should be able to be fold to the left side to have more space for the chat. The fold button should be on the top of the left panel and should be visible at all times. 
- Rename Sessions Panel on Left Side in Active Sessions.
- Make a Skill Button in the left Panel und add a Tab named "Skills" in the right main section. When the user clicks on the Skill Button, a new Panel with skills will be shown (same as the skills section on the history page). The Skills Section on the history page should be removed, it is not needed any more.
- Make sure the metadata for each line in an active session is shown in the chat and updated immediately when the data is there (given the view of the metadata is active). 
- On the server tab make a new dropdown for servertype: "LM Studio, vllm, other". It is planned to give us information which metadata can be called via the server api
- build an adapter to request Metadata from LM Studio servers. Find out how this is best be done.