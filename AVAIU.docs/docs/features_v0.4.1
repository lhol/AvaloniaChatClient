Behebe folgende Probleme:
1. Die aktive Chat Session im linksseitigen Menü wird nicht farbig hervorgehoben, was die Navigation erschwert.
2. Beue die Historie so um, wie die Skills. Also mit Liste der Historien und Details und Aktionsbuttons dann auf der rechten Seiten nach Auswahl.
3. Konzipiere die Metadaten zum Chat neu: Der Chat selbst soll nur den Chat beinhalten, die Metadaten müssen separat abgelegt sein. Sie sollen aber nach jeder
Nachricht aktuell angezeigt werden können, nicht erst in der Historie.