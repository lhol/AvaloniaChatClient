Already implemented
•	Make sure Metadata appears always life as part of the chat window, not only after having the history reloaded again.
•	Layout the Session Tabs to the left, perhaps by using a list or even better a tree view where I can group them under topics.
To be fixed:
*   The active Session should be highlighted in the Session List (e.g. bold font, different background color).
*   When I right click on a session and choose to edit name and comment activate that session in the chat window
	* This is especially important if I have the session list on the left side and not visible all the time. Otherwise, I have to search for the session in the list after editing it, which is not user-friendly.
*   Enhance the Dialog for editing session name and comment:
	* be able to add a topic to the session, which will be shown in the session list and can be used to group sessions (e.g. "Project A", "Project B", "Fun Chats", etc.)
	* if the topic has "/", then make it a subtopic and show it in a tree view in the session list (e.g. "Project A/Meeting Notes", "Project A/Ideas", "Project B/Research", etc.)
	* also add the comment and topic to the dialog when creating a new session, so that I can set them right away and not have to edit the session afterwards to add them.
	* In the topic field, show a dropdown of existing topics for easy selection and consistency.